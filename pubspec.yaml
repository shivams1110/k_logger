name: k_logger
description: Very simple and minimal setup to use, and differentiate logs with colorful output.
version: 0.0.1
repository: https://github.com/shivams1110/k_logger

environment:
  sdk: '>=2.19.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
